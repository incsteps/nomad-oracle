#cloud-config
package_upgrade: true
packages:
  - curl
  - wget
  - unzip
  - jq

runcmd:
  # Install Docker
  - |
    sudo apt-get update
    sudo apt-get install -y ca-certificates curl
    sudo install -m 0755 -d /etc/apt/keyrings
    sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
    sudo chmod a+r /etc/apt/keyrings/docker.asc
    
    echo \
    "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
    $(. /etc/os-release && echo "$UBUNTU_CODENAME") stable" | \
    sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
    sudo apt-get update
    
    sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

  # Download and install Nomad
  - |
    wget -q https://releases.hashicorp.com/nomad/${nomad_version}/nomad_${nomad_version}_linux_arm64.zip
    unzip nomad_${nomad_version}_linux_arm64.zip
    sudo mv nomad /usr/local/bin/
    sudo chmod +x /usr/local/bin/nomad
    sudo mkdir -p /etc/nomad.d /opt/nomad/data    
    sudo chmod -R 755 /etc/nomad.d/
    sudo chmod -R 755 /opt/nomad/data

  # Get instance IPs for configuration
  - |
    PRIVATE_IP=$(hostname -I | awk '{print $1}')
    PUBLIC_IP=$(curl -s http://169.254.169.254/opc/v1/instance/metadata | jq -r '.publicIp // empty')
    
    if [ -z "$PUBLIC_IP" ]; then
      PUBLIC_IP=$PRIVATE_IP
    fi
    
    sed -i "s|__PRIVATE_IP__|$PRIVATE_IP|g" /etc/nomad.d/nomad.hcl
    sed -i "s|__PUBLIC_IP__|$PUBLIC_IP|g" /etc/nomad.d/nomad.hcl

  # Configure firewall
  - |
    sudo iptables -I INPUT -p tcp --dport 4646 -j ACCEPT
    sudo iptables -I INPUT -p tcp --dport 4647 -j ACCEPT
    sudo iptables -I INPUT -p tcp --dport 4648 -j ACCEPT
    sudo iptables -I INPUT -p udp --dport 4648 -j ACCEPT

  # Start services
  - |
    sudo systemctl daemon-reload
    sudo systemctl restart docker
    sudo systemctl enable nomad.service
    sudo systemctl start nomad.service

write_files:
  # Nomad Server Configuration
  - path: /etc/nomad.d/nomad.hcl
    permissions: "0644"
    owner: root:root
    content: |
      data_dir = "/opt/nomad/data"
      bind_addr = "0.0.0.0"
      
      advertise {
        http = "__PRIVATE_IP__:4646"
        rpc  = "__PRIVATE_IP__:4647"
        serf = "__PRIVATE_IP__:4648"
      }
      
      server {
        enabled = true
        bootstrap_expect = 1
      }
      
      client {
        enabled = false
      }
      
      plugin "docker" {
        config {
          allow_privileged = true
          volumes {
            enabled = true
          }
        }
      }

  # Systemd service for Nomad
  - path: /etc/systemd/system/nomad.service
    permissions: "0644"
    owner: root:root
    content: |
      [Unit]
      Description=HashiCorp Nomad
      Documentation=https://www.nomadproject.io/
      Requires=network-online.target
      After=network-online.target docker.service
      
      [Service]
      User=root
      Group=root
      ExecStart=/usr/local/bin/nomad agent -config=/etc/nomad.d
      ExecReload=/bin/kill -HUP $MAINPID
      KillMode=process
      KillSignal=SIGINT
      LimitNOFILE=65536
      LimitNPROC=infinity
      Restart=on-failure
      RestartSec=2
      TasksMax=infinity
      OOMScoreAdjust=-1000
      
      [Install]
      WantedBy=multi-user.target

final_message: "Nomad server configured and started!"
