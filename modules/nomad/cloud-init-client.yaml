#cloud-config
package_upgrade: true
packages:
  - curl
  - wget
  - unzip
  - jq

runcmd:
  # Install Docker
  - |
    sudo apt-get update
    sudo apt-get install -y ca-certificates curl
    sudo install -m 0755 -d /etc/apt/keyrings
    sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
    sudo chmod a+r /etc/apt/keyrings/docker.asc
    
    echo \
    "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
    $(. /etc/os-release && echo "$UBUNTU_CODENAME") stable" | \
    sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
    sudo apt-get update
    
    sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

  # Download and install Nomad
  - |
    wget -q https://releases.hashicorp.com/nomad/${nomad_version}/nomad_${nomad_version}_linux_amd64.zip
    unzip nomad_${nomad_version}_linux_amd64.zip
    sudo mv nomad /usr/local/bin/
    sudo chmod +x /usr/local/bin/nomad
    sudo mkdir -p /etc/nomad.d /opt/nomad/data    
    sudo chmod -R 755 /etc/nomad.d/
    sudo chmod -R 755 /opt/nomad/data

  # Configure Nomad with server IP
  - |
    PRIVATE_IP=$(hostname -I | awk '{print $1}')
    sed -i "s|__PRIVATE_IP__|$PRIVATE_IP|g" /etc/nomad.d/nomad.hcl
    sed -i "s|__SERVER_IP__|${nomad_server_ip}|g" /etc/nomad.d/nomad.hcl

  # Start services (token will be set via cloud-init write_files)
  - |
    sudo systemctl daemon-reload
    sudo systemctl restart docker
    sudo systemctl enable nomad.service
    sudo systemctl start nomad.service

write_files:
  # Nomad Client Configuration
  - path: /etc/nomad.d/nomad.hcl
    permissions: "0644"
    owner: root:root
    content: |
      data_dir = "/opt/nomad/data"
      bind_addr = "0.0.0.0"
      
      advertise {
        http = "__PRIVATE_IP__:4646"
        rpc  = "__PRIVATE_IP__:4647"
        serf = "__PRIVATE_IP__:4648"
      }
      
      client {
        enabled = true
        servers = ["__SERVER_IP__:4647"]
        options {
          "driver.docker.enabled" = "true"
        }
      }
      
      acl {
        enabled = true
      }
      
      plugin "docker" {
        config {
          allow_privileged = true
          volumes {
            enabled = true
          }
        }
      }

  # Systemd service for Nomad (with token sourcing)
  - path: /etc/systemd/system/nomad.service
    permissions: "0644"
    owner: root:root
    content: |
      [Unit]
      Description=HashiCorp Nomad
      Documentation=https://www.nomadproject.io/
      Requires=network-online.target
      After=network-online.target docker.service
      
      [Service]
      User=root
      Group=root
      EnvironmentFile=-/etc/nomad.d/secrets/client-token.env
      ExecStartPre=/bin/bash -c 'if [ -f /etc/nomad.d/secrets/client-token.txt ]; then echo "NOMAD_TOKEN=$(cat /etc/nomad.d/secrets/client-token.txt)" > /etc/nomad.d/secrets/client-token.env; fi'
      ExecStart=/usr/local/bin/nomad agent -config=/etc/nomad.d
      ExecReload=/bin/kill -HUP $MAINPID
      KillMode=process
      KillSignal=SIGINT
      LimitNOFILE=65536
      LimitNPROC=infinity
      Restart=on-failure
      RestartSec=2
      TasksMax=infinity
      
      [Install]
      WantedBy=multi-user.target

final_message: "Nomad client configured and started!"
